<Application x:Class="QbReclass.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:QbReclass.App"
             StartupUri="MainWindow.xaml">
  <Application.Resources>

    <local:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    <local:StringToVisibilityConverter x:Key="StringToVisibility" />

    <!--
      Accounts show their QuickBooks type alongside the name. The rule only makes sense against an
      expense-side account, and naming the type in the list is how someone spots that they have
      picked a bank or income account before the validation message tells them.
    -->
    <DataTemplate x:Key="AccountItem">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding FullName}" />
        <TextBlock Text="{Binding AccountType}" Foreground="#777777" Margin="10,0,0,0" />
        <TextBlock Text="inactive" Foreground="#B00000" Margin="8,0,0,0" FontStyle="Italic">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Visibility" Value="Collapsed" />
              <Style.Triggers>
                <DataTrigger Binding="{Binding IsActive}" Value="False">
                  <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
        </TextBlock>
      </StackPanel>
    </DataTemplate>

    <Style TargetType="TextBlock" x:Key="SectionHeading">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>

    <Style TargetType="TextBlock" x:Key="FieldLabel">
      <Setter Property="Margin" Value="0,0,8,4" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style TargetType="Button">
      <Setter Property="Padding" Value="12,5" />
      <Setter Property="Margin" Value="0,0,8,0" />
      <Setter Property="MinWidth" Value="90" />
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Margin" Value="0,3,12,3" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

  </Application.Resources>
</Application>
